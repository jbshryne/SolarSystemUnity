using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class WideShot : MonoBehaviour
{
    //CinemachineOrbitalTransposer camera;

    // Start is called before the first frame update
    void Start()
    {
        //camera = GetComponent<CinemachineOrbitalTransposer>();
        //Debug.Log(camera);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
